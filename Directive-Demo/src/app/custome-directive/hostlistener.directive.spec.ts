import { HostlistenerDirective } from './hostlistener.directive';

describe('HostlistenerDirective', () => {
  it('should create an instance', () => {
    const directive = new HostlistenerDirective();
    expect(directive).toBeTruthy();
  });
});
